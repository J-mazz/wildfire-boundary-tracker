[project]
name = "wildfire-nrtdv-pipeline"
version = "0.1.0"
description = "Offline geospatial preparation for wildfire-boundary-tracker"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
  "numpy>=2.0",
  "pillow>=11.0",
  "rasterio>=1.4",
]

[tool.uv]
package = false

# Python is limited to offline geospatial preparation; inference is native ncnn/Vulkan.

